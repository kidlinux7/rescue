<%- include('partials/Header') %> 
<div class="homePageContainer">
  <div class="sideBar">
    <!-- <% if(currentUser.type !== 'contentManager') {%>
      <a href="/user-management" class="special"><h6><%= __('Ongeza Mtumiaji Mpya')%></h6></a>
    <% } %> -->
    <%- include('partials/SideTab') %> 
  </div>
  <div class="mainContentContainer">
    <div class="homeTopTextContainer">
      <h2><%= __('Sheria na Masharti')%></h2>
      <button
        type="button"
        class="btn btn-primary "
        style="display: none; background-color: indianred;"
        id="cancelButton"
      >
      <%= __('Ghairi')%>
      </button>
      <button
        type="button"
        class="btn btn-primary btnEditMode"
        id="btnEditMode"
      >
      <%= __('Hariri')%>
      </button>
    </div>
    <%- include('partials/TextEditor', { documents: termsAndConditionsVersions } );%>
  <script type="application/javascript" src="/scripts/termsAndConditionsViewScript.js"></script>
</div>
<%- include('partials/Footer') %>
