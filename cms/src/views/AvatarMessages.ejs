<%- include('modals/InfoModal') %>

<%- include('partials/Header') %> 
<%- include('modals/DynamicModal', {headings: [__('Maudhui')], errors: ['max 40 characters']})%>
<div class="homePageContainer">
  <div class="sideBar">
    <!-- <% if(currentUser.type !== 'contentManager') {%>
      <a href="/user-management" class="special"><h6><%= __('Ongeza Mtumiaji Mpya')%></h6></a>
    <% } %>     -->
    <%- include('partials/SideTab') %> 
  </div>
  <div class="mainContentContainer">
    <div class="homeTopTextContainer">
      <h2><%= __('Meseji za Avatar')%></h2>
      <button
        type="button"
        class="btn btn-primary"
        data-toggle="modal"
        data-target="#dynamicModal"
        data-value="0"
      >
      <%= __('Ongeza meseji za Avatar')%>
      </button>
    </div>
    <div class="homeButtonContainer"></div>
    <div class="homeTableContainer">
      <table class="table table-striped table-borderless tableFixHead table-responsive">
        <thead>
          <tr>
            <th style="width: 55%" class="pointer" id="avatarMessageContent"><%= __('Maudhui')%></th>
            <th style="width: 5%"><%= __('Hariri')%></th>
            <th style="width: 5%"><%= __('Futa')%></th>
            <th style="width: 5%"><%= __('Mbashara')%></th>
          </tr>
        </thead>
        <tbody id="avatarMessageContainer"  style="overflow-y: scroll">
          <% avatarMessages.forEach((item) =>{ %>
          <tr>
            <td><%= item.content %></td>
            <td>
              <button
                type="button"
                class="btn"
                data-toggle="modal"
                data-target="#dynamicModal"
                data-value="<%= item.id %>"
              >
              <i class="fas fa-edit" aria-hidden="true"></i>
              </button>
            </td>
            <td>
              <button type="button" class="btn deleteAvatarMessage" data-value="<%= item.id %>">
                <i class="fas fa-trash" aria-hidden="true"></i>
              </button>
            </td>
            <td>
              <label class="switch">
                <input data-value="<%= item.id %>" class='liveCheckbox' type="checkbox" 
                <%if(item.live) {%>
                  checked
                <% }%>
                />
                <span class="slider round"></span>
              </label>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
  <span id="avatarMessagesJSON" hidden>
    <%= JSON.stringify(avatarMessages); %>
  </span>
  <script type="application/javascript" src="/scripts/avatarMessagesViewScript.js"></script>
</div>
<%- include('partials/Footer') %>
